import { SwipeDrawerDirective } from './swipe-drawer.directive';

describe('SwipeDrawerDirective', () => {
  it('should create an instance', () => {
    const directive = new SwipeDrawerDirective();
    expect(directive).toBeTruthy();
  });
});
